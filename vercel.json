{
  "crons": [
    {
      "path": "/api/cron/gasto-diario",
      "schedule": "0 11 * * *"
    }
  ],
  "ignoreCommand": "git cat-file -e ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} 2>/dev/null || exit 1; git diff --quiet ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} HEAD -- . ':(exclude)docs' ':(exclude)*.md' ':(exclude)scripts/data'",
  "regions": [
    "gru1"
  ]
}
