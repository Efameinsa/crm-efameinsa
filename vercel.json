{
  "crons": [
    {
      "path": "/api/cron/gasto-diario",
      "schedule": "0 11 * * *"
    }
  ],
  "ignoreCommand": "git diff --quiet ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} HEAD -- . ':(exclude)docs' ':(exclude)*.md' ':(exclude)scripts/data'",
  "regions": [
    "gru1"
  ]
}
