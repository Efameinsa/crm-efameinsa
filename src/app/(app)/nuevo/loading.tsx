import { CargaPropuesta } from "@/components/propuesta/carga-propuesta";

// La forma de la pantalla que viene, al instante (ver carga-propuesta).
export default function Loading() {
  return <CargaPropuesta />;
}
